import { DirectivefirstDirective } from './directivefirst.directive';

describe('DirectivefirstDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivefirstDirective();
    expect(directive).toBeTruthy();
  });
});
